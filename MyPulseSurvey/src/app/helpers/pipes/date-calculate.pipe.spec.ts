import { DateCalculatePipe } from './date-calculate.pipe';

describe('DateCalculatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateCalculatePipe();
    expect(pipe).toBeTruthy();
  });
});
