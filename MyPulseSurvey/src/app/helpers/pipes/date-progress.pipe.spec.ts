import { DateProgressPipe } from './date-progress.pipe';

describe('DateProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new DateProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
