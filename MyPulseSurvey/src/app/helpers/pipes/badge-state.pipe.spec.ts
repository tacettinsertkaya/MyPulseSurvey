import { BadgeStatePipe } from './badge-state.pipe';

describe('BadgeStatePipe', () => {
  it('create an instance', () => {
    const pipe = new BadgeStatePipe();
    expect(pipe).toBeTruthy();
  });
});
