import { IdGeneratePipe } from './id-generate.pipe';

describe('IdGeneratePipe', () => {
  it('create an instance', () => {
    const pipe = new IdGeneratePipe();
    expect(pipe).toBeTruthy();
  });
});
