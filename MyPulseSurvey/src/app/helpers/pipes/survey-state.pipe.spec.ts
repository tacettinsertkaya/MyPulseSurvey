import { SurveyStatePipe } from './survey-state.pipe';

describe('SurveyStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SurveyStatePipe();
    expect(pipe).toBeTruthy();
  });
});
